import DeepDiveReportPage from "./DeepDiveReportPage";

/** Client entry point — single-profile, user-only view. */
export default function DeepDiveUserReport() {
  return <DeepDiveReportPage mode="user" />;
}
