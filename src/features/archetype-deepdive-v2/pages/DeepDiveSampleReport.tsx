import DeepDiveReportPage from "./DeepDiveReportPage";

/** Admin entry point — full profile selector + admin tab. */
export default function DeepDiveSampleReport() {
  return <DeepDiveReportPage mode="admin" />;
}
