import ToolboxManagement from "@/pages/admin/ToolboxManagement";

/** Cross-user assignment tracking (list view only). */
export default function ToolboxAssignments() {
  return <ToolboxManagement viewMode="assignments" />;
}
