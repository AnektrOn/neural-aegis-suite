# Toolbox content (Markdown)

One `.md` file = one toolbox item. Import via **Admin → Toolbox → Studio → Import MD**.

See plan spec: frontmatter `distribution.mode` = `catalog` | `individual` | `group` | `global`.

Template: `_templates/toolbox-item-template.md`
